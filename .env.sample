# -----------------------------------------------------------------------------
# DATABASE (Prisma)
# -----------------------------------------------------------------------------
# URL for your database connection. This is used by Prisma to connect to your
# database.
#
# Examples:
# - PostgreSQL: "postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
# - SQLite:     "file:./dev.db"
#
DATABASE_URL="file:./dev.db"


# -----------------------------------------------------------------------------
# EMAIL (React Email / Nodemailer)
# -----------------------------------------------------------------------------
# These variables are for the contact form to send emails.
# You can use a service like Resend, SendGrid, or your own SMTP server.
#
# If using Resend:
# RESEND_API_KEY=your_resend_api_key
#
# If using a generic SMTP server (like Gmail, Outlook, etc.):
# SMTP_HOST=your_smtp_host
# SMTP_PORT=your_smtp_port
# SMTP_USER=your_smtp_username
# SMTP_PASSWORD=your_smtp_password

# The email address that will receive messages from the contact form.
SENDER_EMAIL=your-email@example.com

# The name associated with the sender email.
SENDER_NAME="Your Name"


# -----------------------------------------------------------------------------
# OPENAI (Chat Assistant)
# -----------------------------------------------------------------------------
# API key for the OpenAI API, used for the chat assistant feature.
# You can get this from your OpenAI dashboard.
#
OPENAI_API_KEY="your_openai_api_key"

# -----------------------------------------------------------------------------
# ANALYTICS (Optional)
# -----------------------------------------------------------------------------
# If you are using a service like Vercel Analytics, you might not need an
# environment variable here.
# If using a third-party service like Google Analytics, you would add its
# tracking ID here.
#
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"